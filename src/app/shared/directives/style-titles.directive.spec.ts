import { StyleTitlesDirective } from './style-titles.directive';

describe('StyleTitlesDirective', () => {
  it('should create an instance', () => {
    const directive = new StyleTitlesDirective();
    expect(directive).toBeTruthy();
  });
});
